import { GetDownloadUrlPipe } from './get-download-url.pipe';

describe('GetDownloadUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDownloadUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
